sum = 0
cout =0
while n>=m :
    v1 = v%(2^m)
    v = v>>m
    cout+=1
    n-=m
    Mul = (v1*m)<<(cout*m)
    sum+=Mul



reversed(a[0:k-1])
# 将a中从索引0到索引k-1的元素反转
reversed(a[k:n-1])
# 将a中从索引k到索引n的元素反转
reversed(a[0:n-1])


